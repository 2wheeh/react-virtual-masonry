---
'react-virtual-masonry': minor
---

Add opt-in SSR rendering: pass `ssr={{ itemCount, scrollMargin, columnsCount }}` to render the first N items positioned in server HTML, using the same lane-assignment path the client uses.
