# Demo [React Virtual Masonry in action]

Resize the window to see the lane count respond — 1 lane below 750px, 2 lanes up to 900px, 3 lanes beyond. Only the items around the viewport are mounted; scroll and watch the DOM if you're curious.

import { DemoMasonry } from '../../components/DemoMasonry';

<DemoMasonry />
