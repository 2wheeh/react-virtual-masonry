# Demo [React Virtual Masonry in action]

Drag the width slider to resize the preview _container_ — lane count is driven by `@container` queries reading the container's own size, not the browser viewport. Try the item count selector to see virtualization keep the DOM small even at 20,000 rows, and scroll the page to watch mounted item indexes shift.

import { DemoPlayground } from '../../components/DemoPlayground';

<DemoPlayground />
