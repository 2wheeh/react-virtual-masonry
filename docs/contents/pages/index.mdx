---
title: React Virtual Masonry - Modern Masonry Layout
layout: landing
showLogo: false
---

import { HomePage } from 'vocs/components';

<HomePage.Root>
  <HomePage.Logo />
  <HomePage.Tagline>
    Modern Masonry Layout with ease, powered by @tanstack/virtual.
  </HomePage.Tagline>
  <HomePage.InstallPackage name="react-virtual-masonry @tanstack/react-virtual" type="add" />
  <HomePage.Buttons>
    <HomePage.Button href="/docs" variant="accent">
      Get started
    </HomePage.Button>
    <HomePage.Button href="https://github.com/2wheeh/react-virtual-masonry">GitHub</HomePage.Button>
  </HomePage.Buttons>
</HomePage.Root>

# Overview

```tsx twoslash
import { Masonry } from 'react-virtual-masonry';

const HEIGHTS = [200, 600, 200, 400, 100, 100, 50, 200];

<Masonry
  data={HEIGHTS}
  ssr={{ itemCount: HEIGHTS.length }}
  estimateSize={(i) => HEIGHTS[i]}
  renderItem={({ item, index }) => (
    <div
      style={{
        height: item,
        background: '#F47067',
        color: 'white',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      {index}
    </div>
  )}
/>;
```

import { Masonry } from 'react-virtual-masonry';

export const HEIGHTS = [200, 600, 200, 400, 100, 100, 50, 200];

<Masonry
  data={HEIGHTS}
  ssr={{ itemCount: HEIGHTS.length }}
  estimateSize={(i) => HEIGHTS[i]}
  renderItem={({ item, index }) => (
    <div
      style={{
        height: item,
        background: '#F47067',
        color: 'white',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      {index}
    </div>
  )}
/>
